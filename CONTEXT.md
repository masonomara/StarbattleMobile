The `/packs` folder structure is test data. It was generated by `pack-gen.ts` which was a file generated by AI. We will revisit the bundle structure later, for this phase we just need valid puzzles to play. The puzzles used the generator to be created so they are valid. This means the puzzles are valid, the structure is impermanent.

`/types are subject to change, and are expected to as we develop more. We probably wont get it right the first time. Lets try to stay consistent/not messy, these will serve as a soruce of truth.

`theme` is also subject to change, but looks fine for now

`useTheme seems simple, I like it

`RootNavigator` is fine

we shoudlnt have a types file in navigation, all types go to shared types folder

all the screens are fine for now, focus is ont eh game UI nto the screens ui right now. fine for scaffolding need to be compeltel revisitied
